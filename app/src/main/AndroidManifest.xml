<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
      Handing a downloaded APK to Android's package installer. This permission only buys the right
      to *ask*: the installer still shows its own confirmation, and on Android 8 and later the user
      has to have allowed this app to install packages at all, in a settings screen no app can
      raise on its own. The app is distributed outside any store, so without this an update means
      finding the release page in a browser.
    -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!--
      Split tunnelling has to list the apps that can originate traffic, and on Android 11+ that
      needs visibility of them.

      This stays a blanket permission rather than a <queries> element because there is no query
      form that expresses "every app holding INTERNET": <queries> takes <package>, <intent> and
      <provider>, and nothing else. Narrowing it to <intent> with the LAUNCHER category would drop
      every app without a launcher icon out of the list, which is exactly the kind of background
      sync client somebody wants to route around the tunnel.

      The cost is a Google Play submission would need a written justification for it. The app is
      distributed from GitHub releases, so that cost is currently zero — but it is the one thing
      here that would have to be argued if that ever changed.
    -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Reading a server's QR code. Optional: the phone stays usable without a camera. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".MyDropApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.MyDrop.Splash">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|density|smallestScreenSize"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.MyDrop.Splash">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Happ-compatible deep links: happ://add/<base64url-subscription-url> etc. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="happ" />
                <data android:scheme="mydrop" />
            </intent-filter>

            <!--
              Single proxy links shared from other apps / QR scanners. Every scheme
              ProxyUriParser understands is listed except http/https: catching those would put
              this app in the chooser for ordinary web links.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="vless" />
                <data android:scheme="vmess" />
                <data android:scheme="trojan" />
                <data android:scheme="ss" />
                <data android:scheme="hysteria2" />
                <data android:scheme="hy2" />
                <data android:scheme="hysteria" />
                <data android:scheme="hy" />
                <data android:scheme="tuic" />
                <data android:scheme="anytls" />
                <data android:scheme="socks" />
                <data android:scheme="wireguard" />
                <data android:scheme="wg" />
                <data android:scheme="ssh" />
                <data android:scheme="sing-box" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!--
          The tunnel itself: the VpnService that owns the TUN descriptor and the foreground
          notification, and the platform interface the sing-box core calls back into.
        -->
        <service
            android:name=".vpn.MyDropVpnService"
            android:exported="false"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="vpn" />
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

        <!--
          The tunnel on the quick settings panel. BIND_QUICK_SETTINGS_TILE is what stops anything
          but the system shade from binding it, so exported="true" is safe and required.
        -->
        <service
            android:name=".vpn.TunnelTileService"
            android:exported="true"
            android:icon="@drawable/ic_notification"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <!-- Offer the tile the first time the app runs instead of waiting to be found. -->
            <meta-data
                android:name="android.service.quicksettings.ACTIVE_TILE"
                android:value="false" />
        </service>

        <!--
          Auto-connect after a reboot. BOOT_COMPLETED is one of the few broadcasts still allowed
          to start a foreground service from the background, which is what makes this workable.
        -->
        <receiver
            android:name=".vpn.BootReceiver"
            android:exported="true"
            android:directBootAware="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!--
          The one file the package installer is allowed to read: the APK just downloaded. Scoped
          to a single directory in @xml/update_paths, and not exported.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.updates"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/update_paths" />
        </provider>

    </application>

</manifest>
